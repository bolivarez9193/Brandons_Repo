CREATE TABLE enemies
(
	id				INTEGER			NOT NULL AUTO_INCREMENT PRIMARY KEY,
	priority		INTEGER			NOT NULL DEFAULT 3,
	username		VARCHAR(64)		NOT NULL,
	note			VARCHAR(1024)
);

-- data for testing
INSERT INTO enemies (priority, username, note)
VALUES( 3, 'caalaniz1', "laravel..." );
INSERT INTO enemies (priority, username, note)
VALUES( 3, 'jlfranke', "too many questions. suspcious." );
INSERT INTO enemies (priority, username, note)
VALUES( 2, 'rcantua', "too relaxed." );
INSERT INTO enemies (priority, username, note)
VALUES( 1, 'mamaldonado6', "LATE MAKKE ANRGR...ANGRU...ASNAAKSdkjsfgklaj" );
INSERT INTO enemies (priority, username, note)
VALUES( 1, 'ctchalk', "thinks tiles are more important." );

